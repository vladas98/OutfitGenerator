# LAN IP of the machine running the backend. Update this if your Wi-Fi IP changes
# (check with `ipconfig` on Windows, look for the Wi-Fi adapter's IPv4 Address).
# Phone (Expo Go) and computer must be on the same Wi-Fi network.
EXPO_PUBLIC_API_URL=http://10.0.0.10:4000
